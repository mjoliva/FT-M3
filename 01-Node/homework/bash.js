process.stdout.write("prompt > ");
process.stdin.on("data", (data) => {
  // cuando escribo, leer bien lo que escribo
  // ejecutar una funcion que tenga ese nombre
  // esas funciones, estarán en commands/index.js
});
