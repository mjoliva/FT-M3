// const imprimirNombre = (nombre) => {
//   console.log(nombre);
// };

// const sumar = (a, b) => a + b;

// module.exports = {
//   imprimirNombre,
//   sumar,
// };

module.exports = {
  imprimirNombre: (nombre) => {
    console.log(nombre);
  },

  sumar: (a, b) => a + b,
};
